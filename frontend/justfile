default:
    just --list

storybook:
    pnpm run storybook

install:
    pnpm install

dev:
    pnpm run dev

start:
    pnpm run start

format:
    pnpm exec prettier . --write

check-format:
    pnpm exec prettier . --check

lint:
    pnpm run lint