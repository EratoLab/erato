[package]
name = "erato_mcp_oauth"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"

[dependencies]
futures = "0.3"
http = "1.3.1"
reqwest = { version = "0.13.2", default-features = false, features = ["rustls", "charset", "http2", "system-proxy", "stream"] }
rmcp = { version = "=3.2.0", features = ["auth", "client", "reqwest"] }
serde_json = "1.0"
url = "2.5.8"
