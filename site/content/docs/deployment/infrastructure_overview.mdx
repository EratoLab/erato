# Infrastructure overview

🚧Work in progress 🚧

The main components involved in a typical Erato deployment are:

- The Erato backend (also serves the static frontend bundle)
- PostgreSQL database for persistent storage
- [oauth2-proxy](https://oauth2-proxy.github.io/oauth2-proxy/) for OIDC authentication
