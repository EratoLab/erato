/* This file is generated from the canonical JSON schemas. Do not edit. */

/**
 * Locally matched emails for a query, with an optional locally generated summary. Search and summarization both run entirely on the device.
 */
export interface OutlookSearchEmailsV1Result {
  mailbox: OutlookMailbox;
  /**
   * @maxItems 50
   */
  hits: OutlookSearchHit[];
  /**
   * Number of matching messages before the limit was applied.
   */
  totalMatched?: number;
  /**
   * Plain-text summary of the hits generated by a local model. Absent when summarization was not requested or no local model is available.
   */
  summary?: string;
  /**
   * Identifier of the local model that generated the summary, for user-facing transparency.
   */
  summaryModel?: string;
  /**
   * Search terms actually used after local query expansion.
   *
   * @maxItems 32
   */
  expandedKeywords?: string[];
  /**
   * Local sources or messages that could not be inspected without hiding successful results.
   */
  warnings?: OutlookListingWarning[];
  trace?: SidecarLocalTrace;
  [k: string]: unknown;
}
/**
 * A mailbox or message store available through the local Outlook installation.
 */
export interface OutlookMailbox {
  /**
   * Short opaque mailbox identifier. It is unique for the current sidecar runtime and logically stable across restarts while the Outlook profile and store identity remain unchanged.
   */
  id: string;
  displayName: string;
  emailAddress?: string;
  /**
   * Name of the Outlook profile containing this mailbox. Omitted when the platform or standalone store has no profile concept.
   */
  profileName?: string;
  /**
   * Implementation-defined local Outlook storage source. Known values include pst, ost, macOsProfile, and windowsOutlook.
   */
  source: string;
  [k: string]: unknown;
}
/**
 * One locally matched email for an outlook.search_emails.v1 query.
 */
export interface OutlookSearchHit {
  email: OutlookEmailSummary;
  /**
   * Short plain-text excerpt around the strongest match. Never a full message body.
   */
  snippet?: string;
  /**
   * Fields the query matched. Known values include subject, body, sender, attachmentName, and attachmentContent. Receivers ignore unknown values.
   *
   * @maxItems 16
   */
  matchedIn?:
    | []
    | [string]
    | [string, string]
    | [string, string, string]
    | [string, string, string, string]
    | [string, string, string, string, string]
    | [string, string, string, string, string, string]
    | [string, string, string, string, string, string, string]
    | [string, string, string, string, string, string, string, string]
    | [string, string, string, string, string, string, string, string, string]
    | [string, string, string, string, string, string, string, string, string, string]
    | [string, string, string, string, string, string, string, string, string, string, string]
    | [string, string, string, string, string, string, string, string, string, string, string, string]
    | [string, string, string, string, string, string, string, string, string, string, string, string, string]
    | [string, string, string, string, string, string, string, string, string, string, string, string, string, string]
    | [
        string,
        string,
        string,
        string,
        string,
        string,
        string,
        string,
        string,
        string,
        string,
        string,
        string,
        string,
        string,
      ]
    | [
        string,
        string,
        string,
        string,
        string,
        string,
        string,
        string,
        string,
        string,
        string,
        string,
        string,
        string,
        string,
        string,
      ];
  /**
   * File names of attachments whose name or extracted text matched the query.
   *
   * @maxItems 64
   */
  matchedAttachmentNames?: string[];
  [k: string]: unknown;
}
/**
 * Metadata for one locally indexed Outlook email.
 */
export interface OutlookEmailSummary {
  /**
   * Source-specific stable message identifier.
   */
  id: string;
  subject?: string;
  senderName?: string;
  senderEmailAddress?: string;
  /**
   * UTC Unix timestamp in whole seconds.
   */
  receivedAtUnixSeconds?: number;
  internetMessageId?: string;
  [k: string]: unknown;
}
/**
 * A local Outlook source that could not be inspected without hiding successful results.
 */
export interface OutlookListingWarning {
  path?: string;
  message: string;
  [k: string]: unknown;
}
/**
 * Metadata about the sidecar's internal on-device steps (durations, models, item counts). Never contains message content.
 */
export interface SidecarLocalTrace {
  /**
   * @maxItems 32
   */
  steps: SidecarLocalTraceStep[];
  totalDurationMs?: number;
  [k: string]: unknown;
}
/**
 * One internal on-device processing step, shaped as an event: a stable `sequence` identity carrying a status that may evolve. Metadata only: never message content, snippets, or file names.
 */
export interface SidecarLocalTraceStep {
  /**
   * Stable identity of this step within the request, and its ordering key. A later step with the same sequence supersedes an earlier one, so the same payload works whether the log arrives complete or is appended to over time.
   */
  sequence: number;
  /**
   * Step identifier. Known values include expandQuery, buildIndex, match, and summarize. Receivers ignore unknown values and render them by their raw id.
   */
  id: string;
  /**
   * Step outcome. Known values include running, ok, skipped, degraded, and error. Receivers treat unknown values as running.
   */
  status: string;
  /**
   * Sequence of the step this one runs inside, when the sidecar nests work (for example a tool call made during a local model turn). Absent for top-level steps.
   */
  parentSequence?: number;
  /**
   * Milliseconds between the start of the request and the start of this step, so a client can order and place steps identically in both delivery modes.
   */
  startedAtOffsetMs?: number;
  durationMs?: number;
  /**
   * Identifier of the local model this step used, when it used one.
   */
  model?: string;
  /**
   * Whether this step was served from a local cache (for example the in-memory mailbox index).
   */
  cacheHit?: boolean;
  /**
   * Short non-sensitive note — the sidecar's counterpart of a progress message: why a step was skipped or degraded, or what it is doing.
   */
  detail?: string;
  /**
   * Item counts keyed by an open string. Known keys include keywordsIn, keywordsOut, messagesScanned, matched, and hitsReturned.
   */
  counts?: {
    [k: string]: number;
  };
  [k: string]: unknown;
}
