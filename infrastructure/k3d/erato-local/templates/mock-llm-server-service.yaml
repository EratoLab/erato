{{- if .Values.mockLlmServer.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "erato-local.fullname" . }}-mock-llm-server
  labels:
    app.kubernetes.io/name: mock-llm-server
    app.kubernetes.io/instance: {{ .Release.Name }}
spec:
  type: {{ .Values.mockLlmServer.service.type }}
  selector:
    app.kubernetes.io/name: mock-llm-server
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
    - name: http
      port: {{ .Values.mockLlmServer.service.port }}
      targetPort: http
{{- end }}
