{{- if .Values.mockLlmServer.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "erato-local.fullname" . }}-mock-llm-server
  labels:
    app.kubernetes.io/name: mock-llm-server
    app.kubernetes.io/instance: {{ .Release.Name }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: mock-llm-server
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: mock-llm-server
        app.kubernetes.io/instance: {{ .Release.Name }}
    spec:
      containers:
        - name: mock-llm-server
          image: "{{ .Values.mockLlmServer.image.repository }}:{{ .Values.mockLlmServer.image.tag }}"
          imagePullPolicy: {{ .Values.mockLlmServer.image.pullPolicy }}
          env:
            - name: HOST
              value: "0.0.0.0"
            - name: PORT
              value: {{ .Values.mockLlmServer.service.port | quote }}
            - name: TOKIO_WORKER_THREADS
              value: "4"
          ports:
            - name: http
              containerPort: {{ .Values.mockLlmServer.service.port }}
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 3
            periodSeconds: 5
          resources:
            {{- toYaml .Values.mockLlmServer.resources | nindent 12 }}
{{- end }}
