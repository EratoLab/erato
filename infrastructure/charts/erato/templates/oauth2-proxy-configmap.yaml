{{- if .Values.oauth2Proxy.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-oauth2-proxy
  labels:
    app.kubernetes.io/name: {{ .Release.Name }}-oauth2-proxy
    app.kubernetes.io/instance: {{ .Release.Name }}
data:
  oauth2-proxy.cfg: |-
{{ .Values.oauth2Proxy.config | indent 4 }}
{{- end }} 